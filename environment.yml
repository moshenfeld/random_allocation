name: random_allocation
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.8
  - pip
  - pip:
    # Install the package - dependencies come from pyproject.toml
    - -e .[dev,examples]